package impl;

import spec.Meeting;

/**
 * MeetingImpl is a class implementing {@see Meeting}.
 *
 * @author BBK-PiJ-2016-67
 */
public abstract class MeetingImpl implements Meeting {
/*The class implementing this interface must be abstract. It must have only one constructor
with three parameters: an ID (int), a date, and a set of contacts that must be non-empty
(otherwise, an IllegalArgumentException must be thrown). A IllegalArgumentException
must also be thrown in the case the ID provided was non-positive or zero. If any of the references
/ pointers passed as parameters is null, a NullPointerException must be thrown.*/
}
